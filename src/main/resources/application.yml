spring:
  datasource:
    url: jdbc:mysql://localhost:3306/drink-review?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: Pyh172839!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
        default_batch_fetch_size: 1000

server:
  servlet:
    session:
      timeout: 1800

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace